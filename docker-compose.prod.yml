services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    image: blaue-engel-frontend:prod
    container_name: blaue-engel-frontend
    restart: unless-stopped
    environment:
      API_BASE_URL: https://api-service-app-demo.digitboostsolutions.com
      NEXT_PUBLIC_API_BASE_URL: https://api-service-app-demo.digitboostsolutions.com
      NODE_ENV: production
    networks:
      - proxy

networks:
  proxy:
    external: true
